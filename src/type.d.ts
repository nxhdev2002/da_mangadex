export type ComicState = {
    id: string,
    name: string,
    desc: Array<{}>
    thumb: string,
    currentChap: string,
    createAt: string,
    updatedAt: string,
    availableTranslatedLanguages: Array<string>
}